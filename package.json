{
  "name": "samvidha-login-api",
  "version": "1.0.0",
  "description": "Samvidha Login API using Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "node ./node_modules/playwright/cli.js install chromium",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "playwright": "^1.61.1"
  },
  "engines": {
    "node": "22.x"
  }
}